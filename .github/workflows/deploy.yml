name: Deploy App

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Configure Git Identity
        run: |
          git config --global user.name "Manish Mukhiya"
          git config --global user.email "mukhiyamanish803@gmail.com"

      - name: Deploy to GitHub Pages
        run: npm run deploy
